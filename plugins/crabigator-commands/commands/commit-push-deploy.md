---
description: Commit Crabigator changes, push origin/main, and deploy the Cloudflare Worker.
argument-hint: "[scope]"
allowed-tools: [Bash, Read, Edit]
---

# Crabigator Commit, Push, Deploy

This Codex slash command mirrors the Claude Code `/commit-push-deploy` command.

Read `.claude/commands/commit-push-deploy.md` from the current repo root and follow that workflow exactly. Treat the Claude command file as the canonical source of truth.
