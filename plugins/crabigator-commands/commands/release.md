---
description: Release Crabigator by bumping versions, tagging, pushing, and watching the release workflow.
argument-hint: "[X.Y.Z]"
allowed-tools: [Bash, Read, Edit]
---

# Crabigator Release

This Codex slash command mirrors the Claude Code `/release` command.

Read `.claude/commands/release.md` from the current repo root and follow that workflow exactly. Treat the Claude command file as the canonical source of truth.
