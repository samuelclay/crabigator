---
description: Deploy Crabigator's Cloudflare Worker and dashboard.
argument-hint: "[target]"
allowed-tools: [Bash, Read]
---

# Crabigator Deploy

This Codex slash command mirrors the Claude Code `/deploy` command.

Read `.claude/commands/deploy.md` from the current repo root and follow that workflow exactly. Treat the Claude command file as the canonical source of truth.
