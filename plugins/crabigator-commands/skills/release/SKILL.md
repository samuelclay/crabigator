---
name: release
description: Use when the user asks to release Crabigator, bump the version, create the git tag, and monitor the GitHub release workflow.
---

# Crabigator Release

This Codex skill mirrors the Claude Code `/release` command.

Before acting, read the canonical workflow at `.claude/commands/release.md` from the repo root and follow it exactly. Keep that Claude command file as the source of truth when the workflow changes.
