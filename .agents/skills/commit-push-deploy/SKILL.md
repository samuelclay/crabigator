---
name: commit-push-deploy
description: Use when the user asks to commit Crabigator changes, push them to origin/main, and deploy the Cloudflare Worker.
---

# Crabigator Commit, Push, Deploy

This Codex skill mirrors the Claude Code `/commit-push-deploy` command.

Before acting, read the canonical workflow at `.claude/commands/commit-push-deploy.md` from the repo root and follow it exactly. Keep that Claude command file as the source of truth when the workflow changes.
