---
name: deploy
description: Use when the user asks to deploy Crabigator, publish the dashboard, or push the Worker to Cloudflare.
---

# Crabigator Deploy

This Codex skill mirrors the Claude Code `/deploy` command.

Before acting, read the canonical workflow at `.claude/commands/deploy.md` from the repo root and follow it exactly. Keep that Claude command file as the source of truth when the workflow changes.
